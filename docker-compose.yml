services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - db_data:/app/data  # THÊM volume để persist database
      - vosk_models:/app/data/models  # Volume riêng cho Vosk models
      # Optional: Mount local models if available
      # - ./backend_vosk_vi:/app/data/models/vosk-vi:ro
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
      - VOSK_MODEL_PATH=/app/data/models/vosk-vi
      - DOWNLOAD_MODELS_ON_START=true
    depends_on:
      - db_init
    restart: unless-stopped

  db_init:
    build: ./backend
    command: python -c "
      import sys; sys.path.append('/app'); 
      from config.database import init_db; 
      print('Initializing database...'); 
      init_db(); 
      print('Database initialized successfully')"
    volumes:
      - ./backend:/app
      - db_data:/app/data  # THÊM volume để persist database
    environment:
      - PYTHONPATH=/app

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  db_data:  # THÊM named volume cho database persistence
  vosk_models:  # Volume để persist Vosk models